export default function Announcements() {
  return (
    <section className="placeholder-pane">
      <div className="placeholder-eyebrow">Announcements</div>
      <h1 className="placeholder-title">Announcements</h1>
      <p className="placeholder-body">
        Active launcher announcements with per-account dismiss state land in PR 3.
        Backed by a new <code>launcher_announcements</code> table in the game DB.
      </p>
    </section>
  );
}
