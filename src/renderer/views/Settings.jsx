export default function Settings() {
  return (
    <section className="placeholder-pane">
      <div className="placeholder-eyebrow">Settings</div>
      <h1 className="placeholder-title">Settings</h1>
      <p className="placeholder-body">
        Four-item settings panel lands in PR 4: auto-launch on Windows startup,
        close-X behavior, default realm on launch, sign-out. Persisted to
        launcher's <code>userData</code> dir.
      </p>
    </section>
  );
}
