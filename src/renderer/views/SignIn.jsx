export default function SignIn() {
  return (
    <section className="placeholder-pane">
      <div className="placeholder-eyebrow">Sign In</div>
      <h1 className="placeholder-title">Account</h1>
      <p className="placeholder-body">
        Sign-in form lands in PR 2 — Supabase Auth client, JWT stored via keytar,
        footer links to <code>/account/sign-up</code> + <code>/account/forgot-password</code>.
      </p>
    </section>
  );
}
